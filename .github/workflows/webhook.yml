name: Webhook

on:
  repository_dispatch:
    types: [app_deployed]

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: echo "Hello ${{ github.event.client_payload.name }}"
